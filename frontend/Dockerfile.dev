# Development image: Vite dev server with HMR. Source is bind-mounted by compose;
# node_modules lives in an anonymous volume so the host never needs Node.
FROM node:20-slim

WORKDIR /app
COPY package.json ./
RUN npm install

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev"]
